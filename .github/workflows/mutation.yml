name: Mutation Testing

on:
  schedule:
    - cron: "0 2 * * 0"   # Sundays 02:00 UTC
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  mutation:
    timeout-minutes: 30
    uses: FelipeFuhr/ffreis-workflows-go/.github/workflows/go-mutation.yml@706807616c7887b62bf1f5c72050db843add3828
    with:
      packages: "./internal/..."
      threshold: 60
